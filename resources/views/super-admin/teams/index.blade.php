<x-app-layout>
    <x-slot name="header"><div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3"><div><p class="cricket-kicker mb-2">Platform operations</p><h1 class="display-6 fw-bold mb-2">All Teams</h1><p class="text-secondary mb-0">View all teams across the platform.</p></div><a href="{{ route('super-admin.dashboard') }}" class="btn btn-light"><i class="fa-solid fa-arrow-left me-2"></i>Control plane</a></div></x-slot>
    <div class="container pb-5">
        <div class="cricket-surface p-4 mb-4"><form method="GET" class="row g-2 align-items-end"><div class="col-lg-10"><label class="form-label small text-secondary">Search Teams</label><input type="search" name="search" value="{{ $search }}" class="form-control" placeholder="Name or short name"></div><div class="col-lg-2 d-flex gap-2"><button class="btn btn-success flex-grow-1">Filter</button><a href="{{ route('super-admin.teams.index') }}" class="btn btn-light">Reset</a></div></form></div>
        <div class="cricket-surface overflow-hidden"><div class="d-flex justify-content-between align-items-center gap-3 p-4 border-bottom"><div><p class="cricket-kicker mb-1">Team directory</p><h2 class="h4 fw-bold mb-0">All Teams</h2></div><span class="small text-secondary">{{ $teams->total() }} total teams</span></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>ID</th><th>Name</th><th>Short Name</th><th>Tournament</th><th>Status</th></tr></thead><tbody>@forelse($teams as $team)<tr><td>#{{ $team->id }}</td><td><div class="fw-semibold">{{ $team->name }}</div></td><td>{{ $team->short_name }}</td><td>{{ $team->tournament->name ?? 'Unknown' }}</td><td><span class="badge {{ $team->is_active ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $team->is_active ? 'Active' : 'Inactive' }}</span></td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-secondary">No teams match the search.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $teams->links() }}</div>
    </div>
</x-app-layout>
