<x-app-layout>
    <x-slot name="header"><div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3"><div><p class="cricket-kicker mb-2">Platform operations</p><h1 class="display-6 fw-bold mb-2">All Fixtures</h1><p class="text-secondary mb-0">Every scheduled match card across the platform, including standalone custom fixtures.</p></div><a href="{{ route('super-admin.dashboard') }}" class="btn btn-light"><i class="fa-solid fa-arrow-left me-2"></i>Control plane</a></div></x-slot>
    <div class="container pb-5">
        <div class="cricket-surface p-4 mb-4"><form method="GET" class="row g-2 align-items-end"><div class="col-lg-5"><label class="form-label small text-secondary">Search</label><input type="search" name="search" value="{{ $search }}" class="form-control" placeholder="Team name or venue"></div><div class="col-lg-5"><label class="form-label small text-secondary">Lifecycle status</label><select name="status" class="form-select"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected($selectedStatus === $status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-lg-2 d-flex gap-2"><button class="btn btn-success flex-grow-1">Filter</button><a href="{{ route('super-admin.fixtures.index') }}" class="btn btn-light">Reset</a></div></form></div>
        <div class="cricket-surface overflow-hidden"><div class="d-flex justify-content-between align-items-center gap-3 p-4 border-bottom"><div><p class="cricket-kicker mb-1">Fixture directory</p><h2 class="h4 fw-bold mb-0">All Fixtures</h2></div><span class="small text-secondary">{{ $fixtures->total() }} total fixtures</span></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>ID</th><th>Competition</th><th>Teams</th><th>Scheduled</th><th>Venue</th><th>Status</th><th>Match</th></tr></thead><tbody>@forelse($fixtures as $fixture)<tr><td>#{{ $fixture->id }}</td><td>@if($fixture->tournament)<div class="fw-semibold">{{ $fixture->tournament->name }}</div>@else<span class="badge text-bg-secondary">Custom Match</span>@endif</td><td><div class="fw-semibold">{{ $fixture->homeTeam->name ?? 'TBD' }} vs {{ $fixture->awayTeam->name ?? 'TBD' }}</div></td><td class="text-nowrap">{{ $fixture->scheduled_at?->format('d M Y, H:i') }} {{ $fixture->timezone ? '<span class="text-secondary small">'.$fixture->timezone.'</span>' : '' }}</td><td>{{ $fixture->venue ?? '—' }}</td><td><span class="badge {{ $fixture->status === 'in_progress' ? 'text-bg-success' : ($fixture->status === 'completed' ? 'text-bg-dark' : 'text-bg-light') }}">{{ ucfirst($fixture->status) }}</span></td><td>@if($fixture->match)<a href="{{ route('super-admin.matches.index', ['status' => $fixture->match->status]) }}">#{{ $fixture->match->id }}</a>@else<span class="text-secondary">—</span>@endif</td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-secondary">No fixtures match the filters.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $fixtures->links() }}</div>
    </div>
</x-app-layout>
