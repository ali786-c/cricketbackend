<x-app-layout>
    <x-slot name="header"><div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3"><div><p class="cricket-kicker mb-2">Platform operations</p><h1 class="display-6 fw-bold mb-2">All Matches</h1><p class="text-secondary mb-0">Monitor all matches across the platform.</p></div><a href="{{ route('super-admin.dashboard') }}" class="btn btn-light"><i class="fa-solid fa-arrow-left me-2"></i>Control plane</a></div></x-slot>
    <div class="container pb-5">
        <div class="row g-2 mb-4">@foreach(['scheduled' => 'Scheduled', 'toss_pending' => 'Toss Pending', 'live' => 'Live', 'completed' => 'Completed', 'abandoned' => 'Abandoned', 'cancelled' => 'Cancelled'] as $status => $label)<div class="col-6 col-md-4 col-xl-2"><a href="{{ route('super-admin.matches.index', ['status' => $status]) }}" class="cricket-surface p-3 h-100 text-decoration-none d-block"><div class="small text-secondary">{{ $label }}</div><div class="h3 fw-bold text-dark mb-0">{{ $statusCounts[$status] ?? 0 }}</div></a></div>@endforeach</div>
        <div class="cricket-surface p-4 mb-4"><form method="GET" class="row g-2 align-items-end"><div class="col-lg-10"><label class="form-label small text-secondary">Lifecycle status</label><select name="status" class="form-select"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected($selectedStatus === $status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-lg-2 d-flex gap-2"><button class="btn btn-success flex-grow-1">Filter</button><a href="{{ route('super-admin.matches.index') }}" class="btn btn-light">Reset</a></div></form></div>
        <div class="cricket-surface overflow-hidden"><div class="d-flex justify-content-between align-items-center gap-3 p-4 border-bottom"><div><p class="cricket-kicker mb-1">Match directory</p><h2 class="h4 fw-bold mb-0">All Matches</h2></div><span class="small text-secondary">{{ $matches->total() }} total matches</span></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>ID</th><th>Tournament/Type</th><th>Teams</th><th>Status</th><th>Overs</th></tr></thead><tbody>@forelse($matches as $match)<tr><td>#{{ $match->id }}</td><td>@if($match->tournament)<div class="fw-semibold">{{ $match->tournament->name }}</div>@else<span class="badge text-bg-secondary">Custom Match</span>@endif</td><td><div class="fw-semibold">{{ $match->fixture->homeTeam->name ?? 'TBD' }} vs {{ $match->fixture->awayTeam->name ?? 'TBD' }}</div></td><td><span class="badge {{ $match->status === 'live' ? 'text-bg-success' : ($match->status === 'completed' ? 'text-bg-dark' : 'text-bg-light') }}">{{ ucfirst($match->status) }}</span></td><td>{{ $match->overs_per_innings }}</td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-secondary">No matches found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $matches->links() }}</div>
    </div>
</x-app-layout>
