<x-app-layout>
    <x-slot name="header"><div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3"><div><p class="cricket-kicker mb-2">Platform operations</p><h1 class="display-6 fw-bold mb-2">All Players</h1><p class="text-secondary mb-0">View all player profiles across the platform.</p></div><a href="{{ route('super-admin.dashboard') }}" class="btn btn-light"><i class="fa-solid fa-arrow-left me-2"></i>Control plane</a></div></x-slot>
    <div class="container pb-5">
        <div class="cricket-surface p-4 mb-4"><form method="GET" class="row g-2 align-items-end"><div class="col-lg-10"><label class="form-label small text-secondary">Search Players</label><input type="search" name="search" value="{{ $search }}" class="form-control" placeholder="Name or email"></div><div class="col-lg-2 d-flex gap-2"><button class="btn btn-success flex-grow-1">Filter</button><a href="{{ route('super-admin.players.index') }}" class="btn btn-light">Reset</a></div></form></div>
        <div class="cricket-surface overflow-hidden"><div class="d-flex justify-content-between align-items-center gap-3 p-4 border-bottom"><div><p class="cricket-kicker mb-1">Player directory</p><h2 class="h4 fw-bold mb-0">All Players</h2></div><span class="small text-secondary">{{ $players->total() }} total players</span></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>ID</th><th>Name</th><th>Email</th><th>Role/Style</th></tr></thead><tbody>@forelse($players as $player)<tr><td>#{{ $player->id }}</td><td><div class="fw-semibold">{{ $player->full_name }}</div></td><td>{{ $player->user->email ?? 'N/A' }}</td><td><div class="small text-secondary">{{ ucfirst($player->primary_role) }} · {{ ucfirst($player->batting_style) }}</div></td></tr>@empty<tr><td colspan="4" class="text-center py-5 text-secondary">No players match the search.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $players->links() }}</div>
    </div>
</x-app-layout>
